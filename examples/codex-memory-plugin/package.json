{
  "name": "codex-openviking-memory",
  "version": "0.1.0",
  "description": "OpenViking memory plugin for Codex — semantic long-term memory via MCP",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "install:hooks": "node ./scripts/install-codex-hooks.mjs",
    "uninstall:hooks": "node ./scripts/uninstall-codex-hooks.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}

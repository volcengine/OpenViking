# Retention Policy

Archive memories are intended for long-lived recall after explicit session commit.

Short-lived working context should stay in the active session first. Only durable facts,
preferences, or repeated operational patterns should survive into long-term memory.

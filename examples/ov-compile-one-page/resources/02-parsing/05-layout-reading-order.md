# 版面分析与阅读顺序恢复

版面分析把页面拆分为标题、正文、列表、表格、图片、页眉页脚和旁注。阅读顺序不只按坐标从上到下排列，还要识别分栏、跨栏标题和浮动图文。

## 关键规则

- 页眉页脚通过跨页重复特征识别，不进入正文主干；
- 图注优先与距离最近且序号匹配的图片绑定；
- 脚注保留引用关系，但在纯文本输出中放在对应页末；
- 跨页段落使用行末和下页首行语义连续性合并。

## 调试方式

结果中的每个块都包含 `block_id`、`block_type`、`page_no` 和 `bbox`。当出现顺序错误时，应先用块可视化图检查分栏结果，而不是直接修改分块阈值。

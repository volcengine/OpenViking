# 低质量扫描件 OCR 方案

该方案面向复印件、手机拍摄件和老档案。处理链路先完成旋转和透视矫正，再进行去噪、文字区域检测和多语言识别。

## 质量分级

- A 级：文字边缘清晰，可直接识别；
- B 级：存在轻微倾斜或阴影，需要图像增强；
- C 级：存在重度模糊、遮挡或手写批注，需要输出低置信区域供人工复核。

## 输出要求

每个文本片段保留页码、外接矩形和字符级置信度。当页面平均置信度低于 0.72 时，任务仍可成功，但结果必须附带 `review_recommended=true`。

手写内容不应被静默并入印刷体段落；系统应单独标记其区域和识别结果。

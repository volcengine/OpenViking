# 图片、附件与多模态素材处理

文档中的图片、嵌入文件和音视频链接应作为独立素材保留，而不是在纯文本输出中消失。

## 图片

图片输出原文件内定位、尺寸、格式和内容哈希。有图注时保留原图注；无图注时可生成简短描述，但必须标记为模型生成内容。

## 附件

嵌入的文件作为子资源建立关系，并单独进入格式判定和安全检查。父文档中保留附件名、定位和子资源链接。

## 安全限制

不执行嵌入脚本、宏或可执行文件。素材下载必须经过协议白名单、大小上限和超时控制。

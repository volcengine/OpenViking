# 语义分块与检索单元设计

分块的目标是生成能够独立被检索、同时保留必要上下文的知识单元。固定字符数切分只作为最后的超长保护。

## 分块优先级

1. 先按标题层级和文档组件识别自然边界；
2. 表格、代码块、公式块和图注作为不可任意拆分的单元；
3. 超长章节再根据段落语义相似度切分；
4. 每块携带标题路径，无需复制整个上级章节。

## 建议尺寸

通用问答场景的目标范围是 300–800 个中文字或等价 token。条款类文档更强调结构完整性，可以小于该范围；代码和表格则允许更大。

分块结果必须能反向定位到原文档的页码和块编号。

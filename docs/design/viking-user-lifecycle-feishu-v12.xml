<title>viking://user 生命周期完整解析</title>
<blockquote><p>本文档系统梳理 viking://user/{uid}/ 命名空间下从会话写入、记忆提取、消费到维护删除的完整生命周期，重点讲清 <b>session → trajectory → experience</b> 的流转链路。</p></blockquote>
<hr/>

<h1>一、概述</h1>

<h2>1.1 什么是 viking://user</h2>
<p>viking://user/{uid}/ 是 VikingFS 中的<b>用户私有数据根目录</b>，与全局共享的 viking://resources 并列。它承载该用户名下的会话记录、个人记忆资产、用户级技能、对等 Agent 隔离数据、隐私配置、用户设置与私有资源。</p>
<table>
<colgroup><col width="149"/><col width="328"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>可见性</b></p></td><td><p>默认仅该用户可见；默认检索范围 = ["viking://resources", "viking://user/{uid}"]</p></td></tr>
<tr><td><p><b>隔离性</b></p></td><td><p>天然用户级隔离；对等 Agent 数据再经 peers/{peer_id}/ 二次隔离</p></td></tr>
<tr><td><p><b>核心特征</b></p></td><td><p>会话实时落盘 + 提交后两阶段异步记忆提取，把"原始对话"沉淀为"可复用经验"</p></td></tr>
</tbody>
</table>

<h2>1.2 目录结构总览</h2>
<whiteboard src="K1imwhgTnhQHSMbaHDbcvmnqnke"></whiteboard>
<p>完整目录结构树：</p>
<pre lang="text"><code>viking://user/{uid}/<br/>├── sessions/                          # 会话记录（只增不减，原始消息永不删除）<br/>│   └── {session_id}/<br/>│       ├── messages.jsonl             # 活跃消息，JSONL 格式，实时追加<br/>│       ├── .meta.json                 # SessionMeta 元数据<br/>│       ├── history/<br/>│       │   └── archive_NNN/           # 归档消息（超出窗口的原始消息，原样搬运）<br/>│       └── tools/                     # 大工具输出外置（tool_result_store）<br/>│           └── {tool_id}<br/>├── memories/                          # 记忆资产（核心，按 type 配置合并策略）<br/>│   ├── profile.md                     # 用户画像（upsert）<br/>│   ├── identity.md                    # Agent 身份（upsert）<br/>│   ├── soul.md                        # 灵魂性格（upsert）<br/>│   ├── preferences/                   # 偏好记忆（patch 合并）<br/>│   │   └── {category}.md<br/>│   ├── entities/                      # 实体记忆（patch 合并）<br/>│   │   └── {category}/{name}.md<br/>│   ├── events/                        # 事件记忆（immutable，只设一次）<br/>│   │   └── {timestamp}_{slug}.md<br/>│   ├── trajectories/                  # 轨迹记忆（add_only，每次新执行产生新文件）<br/>│   │   └── {name}_{timestamp}.md<br/>│   ├── experiences/                   # 经验记忆（upsert，可复用执行规则）<br/>│   │   └── {name}.md<br/>│   ├── cases/                         # 用例记忆（add_only）<br/>│   │   └── {timestamp}_{slug}.md<br/>│   ├── tools/                         # 工具记忆（upsert）<br/>│   │   └── {tool_name}.md<br/>│   └── skills/                        # 技能统计记忆（upsert，与顶层 skills/ 不同）<br/>│       └── {skill_name}.md<br/>├── skills/                            # 用户显式要求记住的可复用技能<br/>│   └── {skill_name}/SKILL.md<br/>├── peers/                             # 对等 Agent 的隔离数据<br/>│   └── {peer_id}/<br/>│       ├── memories/                  # 对等 Agent 的记忆<br/>│       └── resources/                 # 对等 Agent 的私有资源<br/>├── privacy/                           # 隐私配置 + 版本历史<br/>│   └── {category}/{target_key}<br/>│       ├── current.json               # 当前生效配置<br/>│       └── version_*.json             # 历史版本<br/>├── settings/                          # 用户配置<br/>│   ├── user_config.json               # 主配置（add_targets、memory_policy 等）<br/>│   └── user_config.backup.json        # 备份<br/>└── resources/                         # 用户私有资源（与全局 viking://resources 分开）<br/>    └── ...</code></pre>
<p>viking://user/{uid}/ 下一级子目录一览：</p>
<table>
<colgroup><col width="114"/><col width="328"/><col width="296"/></colgroup>
<thead><tr><th background-color="light-gray"><p>子目录</p></th><th background-color="light-gray"><p>内容</p></th><th background-color="light-gray"><p>关键性质</p></th></tr></thead>
<tbody>
<tr><td><p>sessions/</p></td><td><p>全部会话记录（活跃 + 归档 + 大工具输出外置）</p></td><td><p>只增不减，原始消息永不删除</p></td></tr>
<tr><td><p>memories/</p></td><td><p>记忆资产（画像 / 偏好 / 实体 / 事件 / 轨迹 / 经验 / 用例 / 统计）</p></td><td><p>按 memory type 配置合并策略</p></td></tr>
<tr><td><p>skills/</p></td><td><p>用户显式要求记住的可复用技能（SKILL.md）</p></td><td><p>与 memories/skills/（统计）不同</p></td></tr>
<tr><td><p>peers/</p></td><td><p>对等 Agent 的隔离记忆与资源</p></td><td><p>MemoryIsolationHandler 路由</p></td></tr>
<tr><td><p>privacy/</p></td><td><p>隐私配置 + 版本历史</p></td><td><p>current + version_*.json</p></td></tr>
<tr><td><p>settings/</p></td><td><p>用户配置（add_targets、memory_policy 等）</p></td><td><p>user_config.json</p></td></tr>
<tr><td><p>resources/</p></td><td><p>用户私有资源</p></td><td><p>与全局 resources 分开</p></td></tr>
</tbody>
</table>
<hr/>

<h1>二、sessions/ 子目录生命周期</h1>

<h2>2.1 存储结构</h2>
<p>每个会话一个目录，结构如下：</p>
<pre lang="text"><code>viking://user/{uid}/sessions/{session_id}/<br/>├── messages.jsonl          # 活跃消息，JSONL，实时追加<br/>├── .meta.json               # 会话元数据（SessionMeta）<br/>├── history/<br/>│   └── archive_001/<br/>│       ├── messages.jsonl   # 归档原始消息（完整未截断）<br/>│       ├── .meta.json       # 归档元数据<br/>│       ├── .overview.md     # Working Memory v2 摘要（7段）<br/>│       ├── .abstract.md     # 摘要再提炼<br/>│       ├── .done            # 完成标记<br/>│       └── .failed.json     # 失败标记（可选）<br/>└── tool-results/<br/>    └── tr_{tool_id}_{sha16}/<br/>        ├── output.txt       # 外置大工具输出全文<br/>        └── metadata.json    # 元数据 + synopsis</code></pre>
<p><b>各文件格式要点：</b></p>
<table>
<colgroup><col width="200"/><col width="120"/><col width="410"/></colgroup>
<thead><tr><th background-color="light-gray"><p>文件</p></th><th background-color="light-gray"><p>格式</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p>messages.jsonl</p></td><td><p>JSON Lines</p></td><td><p>每行一条消息（含 role/parts/token 估算），是会话的唯一真相源</p></td></tr>
<tr><td><p>.meta.json</p></td><td><p>JSON</p></td><td><p>SessionMeta：状态、提交计数、auto-commit 策略等</p></td></tr>
<tr><td><p>archive_NNN/messages.jsonl</p></td><td><p>JSON Lines</p></td><td><p>归档时切出的完整原始消息，<b>不做截断</b></p></td></tr>
<tr><td><p>.overview.md</p></td><td><p>Markdown</p></td><td><p>Phase 2 生成的 7 段 Working Memory 摘要</p></td></tr>
<tr><td><p>.abstract.md</p></td><td><p>Markdown</p></td><td><p>摘要的再提炼，用于目录级语义检索</p></td></tr>
<tr><td><p>.done</p></td><td><p>空文件 / JSON</p></td><td><p>Phase 2 完成标记（含 coverage 范围）</p></td></tr>
<tr><td><p>.failed.json</p></td><td><p>JSON</p></td><td><p>Phase 2 失败标记，便于重试排查</p></td></tr>
<tr><td><p>tool-results/output.txt</p></td><td><p>纯文本</p></td><td><p>大工具输出外置全文，消息内仅保留引用</p></td></tr>
</tbody>
</table>

<h2>2.2 写入：创建与实时追加</h2>
<ul>
<li><b>创建会话</b>时初始化 messages.jsonl 与 .meta.json。</li>
<li><b>每条消息</b>通过 append_file 立即追加到根 messages.jsonl，写入时持有 AGFS PathLock，保证并发安全。</li>
<li>大体积工具输出不直接内联进消息，而是外置到 tool-results/tr_{tool_id}_{sha16}/output.txt，消息里只存引用与 synopsis，避免活跃消息体积膨胀。</li>
</ul>

<h2>2.3 自动提交（auto-commit）触发机制</h2>
<p>会话没有显式 policy 时默认不自动提交；policy 在创建时或后续配置 API 设定。触发条件（任一满足即触发一次 commit）：</p>
<table>
<colgroup><col width="240"/><col width="240"/><col width="240"/></colgroup>
<thead><tr><th background-color="light-gray"><p>触发条件</p></th><th background-color="light-gray"><p>默认阈值</p></th><th background-color="light-gray"><p>PRD 上限</p></th></tr></thead>
<tbody>
<tr><td><p>pending_token_threshold</p></td><td><p>150,000 token</p></td><td><p>1,000,000</p></td></tr>
<tr><td><p>message_count_threshold</p></td><td><p>100 条</p></td><td><p>1,000 条</p></td></tr>
<tr><td><p>idle_timeout_seconds</p></td><td><p>86,400 秒（1 天）</p></td><td><p>604,800 秒（7 天）</p></td></tr>
</tbody>
</table>
<p>此外还有 keep_recent_count（保留最近 N 轮）与 min_commit_interval_seconds（最小提交间隔）两个调节项。</p>

<h2>2.4 压缩/归档两阶段协议（Phase 1 + Phase 2）</h2>
<p>提交被拆成<b>同步 Phase 1</b> + <b>异步 Phase 2</b>，通过 QueueFS 解耦。</p>
<p><b>Phase 1（同步持锁）：</b></p>
<ol>
<li>调 retention 规划器在活跃消息上选归档 / 保留边界。</li>
<li>把归档消息完整写入 archive_NNN/messages.jsonl。</li>
<li>把根 messages.jsonl 重写为保留下来的消息（retained）。</li>
<li>把提交任务入队 QueueFS。</li>
</ol>
<p><b>Phase 2（异步 worker，重启安全）：</b></p>
<ol>
<li>hydrate 归档消息中引用的大工具输出全文。</li>
<li><b>并行</b>跑两类任务：Working Memory 摘要 → 写 .overview.md / .abstract.md；长期记忆抽取（含 trajectory / experience 两阶段，见第五章）。</li>
<li>成功写 .done，失败写 .failed.json；会话不存在则直接标记任务失败。</li>
</ol>
<blockquote><p>Phase 1 保证用户侧读写快速响应；Phase 2 慢（依赖 LLM / embedding），放到后台异步完成。</p></blockquote>
<p><b>Session 压缩/摘要 Prompt：</b></p>
<ul>
<li>创建模板：openviking/prompts/templates/compression/ov_wm_v2.yaml</li>
<li>增量模板：ov_wm_v2_update.yaml（增量模式强制通过 update_working_memory 工具按 KEEP / APPEND / REPLACE 合并）</li>
<li>输出固定为 <b>7 段</b> Working Memory Markdown：</li>
</ul>
<ol>
<li>Session Title</li>
<li>Current State（最高优先级，永远反映最新状态）</li>
<li>Task &amp; Goals</li>
<li>Key Facts &amp; Decisions（关于用户世界的持久事实）</li>
<li>Files &amp; Context（被引用的资源）</li>
<li>Errors &amp; Corrections</li>
<li>Open Issues</li>
</ol>

<h2>2.5 保留与删除策略</h2>
<ul>
<li>retention.py 是<b>纯规划器</b>，无文件系统 / 模型依赖：先把物理消息聚合成逻辑 user turn + 原子 assistant step，再按 turn_budget（轮数 + token 双约束）选归档边界。</li>
<li><b>永不拆碎原子单元</b>：tool call 与其结果 transport 消息始终在同一侧，不会跨归档边界割裂。</li>
<li><b>无 TTL</b>：归档目录只增不减，没有过期清理。</li>
<li><b>原始消息永不删除</b>：归档消息完整保存在 archive_NNN/messages.jsonl；"删除"只作用于记忆索引 / 向量，不删原始会话数据。</li>
</ul>
<hr/>

<h1>三、memories/ 子目录全景</h1>

<h2>3.1 memories/ 目录树总览</h2>
<pre lang="text"><code>viking://user/{uid}/memories/<br/>├── profile.md              # 用户画像（单文件，upsert，patch合并）<br/>├── identity.md             # Agent 身份（单文件）<br/>├── soul.md                 # Agent 灵魂/性格（单文件）<br/>├── preferences/            # 用户偏好 {user}/{topic}.md<br/>├── entities/               # 第三方实体卡片 {category}/{name}.md<br/>├── events/                 # 原子事件 {YYYY}/{MM}/{DD}/{name}.md<br/>├── experiences/            # 可复用经验 {name}.md（upsert，重点）<br/>├── trajectories/           # 执行轨迹 {name}_{ts}.md（add_only，重点）<br/>├── cases/                  # 训练/评估用例 {name}.md<br/>├── tools/                   # 工具使用统计（默认 disabled）<br/>└── skills/                  # 技能使用统计（默认 disabled）</code></pre>
<p>每个 memory type 由 openviking/prompts/templates/memory/*.yaml 声明，关键属性如下：</p>
<table>
<colgroup><col width="120"/><col width="220"/><col width="120"/><col width="130"/><col width="180"/></colgroup>
<thead><tr><th background-color="light-gray"><p>memory type</p></th><th background-color="light-gray"><p>目录 / 文件名</p></th><th background-color="light-gray"><p>operation_mode</p></th><th background-color="light-gray"><p>content 合并</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p>profile</p></td><td><p>profile.md</p></td><td><p>upsert</p></td><td><p>patch</p></td><td><p>用户画像</p></td></tr>
<tr><td><p>identity</p></td><td><p>identity.md</p></td><td><p>upsert</p></td><td><p>patch</p></td><td><p>Agent 身份</p></td></tr>
<tr><td><p>soul</p></td><td><p>soul.md</p></td><td><p>upsert</p></td><td><p>patch</p></td><td><p>Agent 性格</p></td></tr>
<tr><td><p>preferences</p></td><td><p>preferences/{user}/{topic}.md</p></td><td><p>upsert</p></td><td><p>patch</p></td><td><p>用户偏好</p></td></tr>
<tr><td><p>entities</p></td><td><p>entities/{category}/{name}.md</p></td><td><p>upsert</p></td><td><p>patch</p></td><td><p>第三方实体卡片</p></td></tr>
<tr><td><p>events</p></td><td><p>events/{YYYY}/{MM}/{DD}/{name}.md</p></td><td><p>add_only</p></td><td><p>immutable 字段</p></td><td><p>原子事件</p></td></tr>
<tr><td><p><b>trajectories</b></p></td><td><p>trajectories/{name}_{ts}.md</p></td><td><p><b>add_only</b></p></td><td><p>patch（正文）</p></td><td><p>执行轨迹契约</p></td></tr>
<tr><td><p><b>experiences</b></p></td><td><p>experiences/{name}.md</p></td><td><p><b>upsert</b></p></td><td><p>replace（正文）</p></td><td><p>可复用经验</p></td></tr>
<tr><td><p>cases</p></td><td><p>cases/{name}.md</p></td><td><p>upsert</p></td><td><p>immutable 字段</p></td><td><p>训练/评估用例</p></td></tr>
<tr><td><p>tools</p></td><td><p>tools/{tool_name}.md</p></td><td><p>upsert</p></td><td><p>sum + patch</p></td><td><p>使用统计，默认 disabled</p></td></tr>
<tr><td><p>skills</p></td><td><p>skills/{skill_name}.md</p></td><td><p>upsert</p></td><td><p>sum + patch</p></td><td><p>使用统计，默认 disabled</p></td></tr>
</tbody>
</table>

<h2>3.2 各子目录逐一介绍</h2>

<h3>3.2.1 profile.md / identity.md / soul.md（单文件）</h3>
<ul>
<li><b>存储内容</b>：整份用户画像（profile）、Agent 身份设定（identity）、Agent 性格/灵魂（soul）。</li>
<li><b>数据格式</b>：单 Markdown 文件，带 MEMORY_FIELDS 元数据（尾部 HTML 注释）。</li>
<li><b>写入方式</b>：operation_mode: upsert，content 字段走 patch（SEARCH/REPLACE/DELETE 块级编辑），首次无文件时按 init_value 模板初始化。</li>
<li><b>消费方式</b>：身份类内容在会话早期注入上下文，作为 Agent 行为基线。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>post-commit 的会话消息文本（SessionExtractContextProvider 组装为 ## Conversation History，默认省略 ToolPart，只保留 TextPart）</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p>profile：只收"跨会话持久的当前状态"（基本身份、学业职业、婚恋家庭、长期健康），强制路由（时间性事件→events、好恶习惯→preferences、第三方对象→entities），每条 bullet 必须以 (as of YYYY-MM-DD) 结尾；identity / soul：极简字段提示（name/creature/vibe/emoji 或 core_truths/boundaries/continuity），多带 init_value 预置</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>单文件 profile.md / identity.md / soul.md；唯一业务字段 content（merge_op=patch）；正文由 content_template 渲染（identity 固定 Name/Creature/Vibe 等区块，soul 固定 Core Truths/Boundaries 等四区块）；元数据含 memory_type / version / links</p></td></tr>
</tbody>
</table>

<h3>3.2.2 preferences/</h3>
<ul>
<li><b>存储内容</b>：按用户 + 主题组织的偏好卡片，文件名 {user}/{topic}.md。</li>
<li><b>数据格式</b>：每主题一个 Markdown 文件；user / topic 字段为 immutable，content 为 patch。</li>
<li><b>写入方式</b>：识别到用户对某主题的稳定偏好时 upsert。</li>
<li><b>消费方式</b>：与当前话题相关时召回注入，指导语气 / 格式 / 决策倾向。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>post-commit 会话消息文本中"用户喜欢/习惯/倾向什么"的表述</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p>每条偏好针对<b>具体话题</b>（代码风格、沟通方式、工具选择、饮食等），不同话题必须拆成不同文件；topic 为小写下划线 ≤3 词，与 user 共同构成主键</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>memories/preferences/{user}/{topic}.md；字段：user（immutable）、topic（immutable）、content（patch，bullet 列表）；有 embedding_template（user+topic+content）用于向量检索</p></td></tr>
</tbody>
</table>

<h3>3.2.3 entities/</h3>
<ul>
<li><b>存储内容</b>：第三方实体卡片（联系人、组织、产品等），文件名 {category}/{name}.md。</li>
<li><b>数据格式</b>：category / name 为 immutable，描述 / 备注为 patch。</li>
<li><b>写入方式</b>：对话中沉淀出稳定实体时创建或 patch 更新。</li>
<li><b>消费方式</b>：按实体名 / 类别语义检索召回。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>会话中提到的真实人 / 组织 / 地点 / 作品 / 产品 / 宠物 / 概念</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p>实体身份 = 现实对象本身，只合并显式别名；不同对象即使同主题也必须分开文件；正文硬性规定 H1 显示名 + 一句无项目符号描述 + 2–4 个 H2（每个 H2 下 1–5 条 bullet，禁止 H2 下写段落）</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>memories/entities/{category|lower}/{name|lower}.md；字段：category（immutable）、name（immutable）、content（patch）</p></td></tr>
</tbody>
</table>

<h3>3.2.4 events/</h3>
<ul>
<li><b>存储内容</b>：原子事件（带时间戳的发生事实），路径 {YYYY}/{MM}/{DD}/{event_name}.md。</li>
<li><b>数据格式</b>：operation_mode: add_only，字段多为 immutable——一旦写入不再修改。</li>
<li><b>写入方式</b>：按事件发生日期落盘，只追加；写入侧走快速路径，不做 patch 合并。</li>
<li><b>消费方式</b>：按时间范围 / 事件语义检索，支撑"最近发生了什么"类上下文。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>post-commit 会话消息；LLM 输出 ranges 字段（如 "0-3,7"）指向消息索引，正文由 content_template 事后从<b>原始消息截取</b>渲染（get_event_content），所以 event 文件自带"当时的对话摘录"</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p><b>原子化</b>——一个事件只记一次发生 / 决定 / 承诺 / 安排；附大量好坏粒度示例；相对时间转具体日期；第三人称；ranges 必须含至少一条 user 消息；禁止 *_chat/_talk 式命名</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>memories/events/{年}/{月}/{日}/{event_name}.md；字段：event_name（immutable）、goal（≤5 词）、summary、ranges（immutable）；add_only 是唯一走快速路径、不做 patch 合并的 user 类型。注意：events 仍有 LLM 抽取（LLM 决定记什么、起什么名、归哪段消息），并非纯机械记录</p></td></tr>
</tbody>
</table>

<h3>3.2.5 trajectories/（重点）</h3>
<ul>
<li><b>存储内容</b>：从一次任务执行中提炼出的<b>可复用操作契约</b>，不是原始步骤流水。</li>
<li><b>数据格式</b>：路径 memories/trajectories/{trajectory_name}_{session_timestamp}.md；operation_mode: add_only——只追加，已写出的轨迹不更新。frontmatter 字段：trajectory_name、outcome（success/failure/partial/unfinished/unknown）、task_query、retrieval_anchor、content。除 content 外字段均为 immutable（只设一次）。</li>
<li><b>正文 12 段操作契约</b>（固定顺序）：Domain / Trigger / Operation Intent（Family + Primary object + Target lifecycle）/ Preconditions / Immutable Object Boundary / Procedure / Write Field Provenance / Anti-patterns / Applicability Boundary / Negative Applicability / Result / Evidence。</li>
<li><b>本质</b>：一条轨迹记录一个"状态-动作边界"——进入条件 → 读/验 → 一次主要写/交接/应答 → 验证收尾，而不是整个 session 的步骤序列。一次 session 含多个可复用边界时拆成多条。</li>
<li><b>写入方式</b>：由 Phase 1 提取器在复杂任务经验沉淀场景中产出（见 5.4）。</li>
<li><b>消费方式</b>：既是 Phase 2 提取经验的原料，也作为 Train 子系统的 rollout 信号。</li>
</ul>
<blockquote><p>详细抽取流程（原始内容 / 抽取提示词 / 抽取结果）见 <b>5.4 场景三：复杂任务经验沉淀</b>。</p></blockquote>

<h3>3.2.6 experiences/（重点）</h3>
<ul>
<li><b>存储内容</b>：可泛化、可复用的执行经验，会被直接注入自治 Agent 的 system prompt。</li>
<li><b>数据格式</b>：路径 memories/experiences/{experience_name}.md；operation_mode: upsert——同名经验整体更新。字段：experience_name（immutable）、content（replace，全量重写）、supersedes（replace，指向被本经验替代的旧经验名）。正文三段式：</li>
</ul>
<ul>
<li>## Situation：进入条件（泛化的上下文 / 用户意图）</li>
<li>## Approach：执行步骤（命令式，IF/THEN/ELSE，只写正面动作）</li>
<li>## Reflect：负面约束（NEVER…、失败防护，只写"不要做什么"）</li>
</ul>
<ul>
<li><b>写入方式</b>：由 Phase 2 提取器在新轨迹到达时，结合向量检索候选决定 Create / Update / Replace / Skip（见 5.4）。</li>
<li><b>消费方式</b>：按 retrieval_anchor / 正文做向量召回，命中后直接注入 system prompt 作为可执行规则。</li>
</ul>
<blockquote><p>详细抽取流程（原始内容 / 抽取提示词 / 抽取结果）见 <b>5.4 场景三：复杂任务经验沉淀</b>。</p></blockquote>

<h3>3.2.7 cases/</h3>
<ul>
<li><b>存储内容</b>：训练 / 评估用例，文件名 {case_name}.md。</li>
<li><b>数据格式</b>：字段全为 immutable，作为固定样本。</li>
<li><b>写入方式</b>：从含足够工具执行 / 结果证据的 commit 中抽取；与 profile / preferences / events 由<b>同一个 ExtractLoop 一并产出</b>，不是独立 LLM 调用。</li>
<li><b>消费方式</b>：Train 子系统加载为 rollout 用例。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>同一次 commit 的会话消息（需含足够的工具执行 / 结果证据）；纯闲聊 / 问答跳过；peer_enabled=false</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p>从真实 commit 抽"可训练 / 可评测"样本——必须有具体用户任务 + 足够执行证据；定义场景与 rubric，供训练管线从 rollout 学经验</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>memories/cases/{case_name}.md；字段全 immutable：case_name、task_signature（泛化任务签名）、input（JSON）、rubric（含 criteria/weight 的 JSON）、evidence；content_template 渲染成 Task Signature / Input / Rubric / Evidence / Linked Experiences 区块</p></td></tr>
</tbody>
</table>

<h3>3.2.8 tools/ 与 skills/（默认 disabled）</h3>
<ul>
<li><b>存储内容</b>：工具 / 技能的使用统计（call_count、success_count 等）。</li>
<li><b>数据格式</b>：统计字段用 sum（数值累加），描述字段用 patch。</li>
<li><b>默认状态</b>：enabled: false，需显式开启才会提取。</li>
<li><b>注意区分</b>：memories/skills/ 是<b>使用统计</b>；viking://user/{uid}/skills/ 是<b>用户记住的可复用技能本体</b>（见 4.1），两者不同。</li>
</ul>
<p><b>抽取流程（三要素）：</b></p>
<table>
<colgroup><col width="120"/><col width="660"/></colgroup>
<thead><tr><th background-color="light-gray"><p>维度</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>原始内容</b></p></td><td><p>工具 / 技能的调用记录（当前默认 disabled，不参与抽取）</p></td></tr>
<tr><td><p><b>抽取提示词</b></p></td><td><p>设计上面向工具 / 技能使用模式沉淀；统计字段（call_count / success_count / total_executions 等）走 merge_op: sum 数值累加，<b>非 LLM 语义抽取</b>；文本字段（when_to_use / optimal_params / common_failures / recommendation / guidelines）走 patch</p></td></tr>
<tr><td><p><b>抽取结果</b></p></td><td><p>memories/tools/{tool_name}.md / memories/skills/{skill_name}.md；当前未启用</p></td></tr>
</tbody>
</table>
<hr/>

<h2>3.3 记忆抽取机制总览</h2>

<h3>3.3.1 两条独立的抽取流水线</h3>
<p>记忆抽取按 stage 分为两条完全独立的流水线，互不交叉：</p>
<table>
<colgroup><col width="130"/><col width="70"/><col width="250"/><col width="150"/><col width="180"/></colgroup>
<thead><tr><th background-color="light-gray"><p>流水线</p></th><th background-color="light-gray"><p>stage</p></th><th background-color="light-gray"><p>覆盖 memory type</p></th><th background-color="light-gray"><p>触发时机</p></th><th background-color="light-gray"><p>编排入口</p></th></tr></thead>
<tbody>
<tr><td><p><b>用户记忆抽取</b></p></td><td><p>user</p></td><td><p>profile / identity / soul / preferences / entities / events / cases</p></td><td><p>每次 session commit 后</p></td><td><p>SessionCompressorV3.extract_long_term_memories()</p></td></tr>
<tr><td><p><b>Agent 记忆抽取</b></p></td><td><p>agent</p></td><td><p>trajectories / experiences</p></td><td><p>Train 管线的 rollout 分析阶段</p></td><td><p>TrajectoryRolloutAnalyzer.analyze() → ExperienceGradientEstimator.estimate()</p></td></tr>
</tbody>
</table>
<p>SessionExtractContextProvider 在加载 schema 时只取 stage=="user" 的启用类型，trajectories / experiences（stage=agent）被自动过滤，不会在用户会话提交时被抽取。</p>

<h3>3.3.2 用户记忆抽取的完整链路</h3>
<pre lang="text"><code>会话 commit/archive 完成<br/>  └─ SessionExtractContextProvider(messages)<br/>       · 组装 "## Conversation History"（默认省略 ToolPart，只保留 TextPart）<br/>       · prefetch：单文件类型（profile/identity/soul）→ 直接 read；<br/>                    多文件类型（entities/preferences）→ 目录语义 search；<br/>                    add_only 类型（events）→ 完全跳过 prefetch<br/>  └─ ExtractLoop.run()  ← 唯一的 LLM 抽取调用（ReAct，默认 max_iterations=3）<br/>       · system = provider instruction + page_id 规则 + read 格式 + 输出契约<br/>       · 输出契约 = SchemaModelGenerator 从 YAML 动态生成的 Pydantic JSON Schema<br/>       · LLM 可调 read/search 工具补读细节，或直接吐出最终 operations<br/>  └─ 输出协议解析（默认 Python DSL，可选 JSON）<br/>       · Python：受限 DSL（sdk.create_* / obj.content.edit() / dup.delete(replacement=)），AST 沙箱编译<br/>       · JSON：严格符合 JSON Schema 的对象，容错解析<br/>  └─ resolve_operations（page_id → URI 绑定、peer 归属、event ranges 解析）<br/>  └─ StreamingMemoryUpdater.submit()<br/>       · 按 (peer_id, memory_type) 做计数/时间窗批处理（默认 ≤8 个 op / 10s）<br/>       · add_only（events）→ 快速路径直接写，不合并<br/>       · 其余类型 → 跨 session/重复目标时，跑第二次 mini-ExtractLoop<br/>         （PatchMergeContextProvider, max_iterations=1, 无工具）合并多份提案<br/>  └─ MemoryUpdater.apply_operations()<br/>       · 按字段 merge_op（patch / replace / sum / immutable）合并写文件<br/>       · 向量化入 VikingDB<br/>       · 生成 .overview.md</code></pre>
<p><b>关键认知</b>：StreamingMemoryUpdater 不是"实时 LLM 抽取"——它是写入侧的批处理 / 合并层，接收的已是抽取完成的 ResolvedOperations。真正的 LLM 抽取统一在 commit 后跑一次，没有逐消息实时抽取。</p>

<h3>3.3.3 两次 LLM 调用的分工</h3>
<table>
<colgroup><col width="120"/><col width="330"/><col width="330"/></colgroup>
<thead><tr><th background-color="light-gray"><p></p></th><th background-color="light-gray"><p>第一次：会话抽取</p></th><th background-color="light-gray"><p>第二次：patch 合并</p></th></tr></thead>
<tbody>
<tr><td><p>Provider</p></td><td><p>SessionExtractContextProvider</p></td><td><p>PatchMergeContextProvider</p></td></tr>
<tr><td><p>输入</p></td><td><p>会话消息文本（Conversation History）</p></td><td><p>若干 before/after 记忆文件的<b>字段级 unified diff</b> + 原文件 read 结果</p></td></tr>
<tr><td><p>触发</p></td><td><p>每次 commit 一次</p></td><td><p>仅当一批操作里<b>多个提案指向同一/重复记忆</b>时</p></td></tr>
<tr><td><p>工具</p></td><td><p>read / search</p></td><td><p>无工具（get_tools()→[]）</p></td></tr>
<tr><td><p>迭代</p></td><td><p>3 轮 ReAct</p></td><td><p>1 轮，纯输出</p></td></tr>
<tr><td><p>Prompt 主题</p></td><td><p>"从对话抽取应写什么记忆"</p></td><td><p>"把多份独立抽取提案调和成唯一 canonical 文件"</p></td></tr>
</tbody>
</table>

<h3>3.3.4 统一的记忆文件格式</h3>
<p>所有记忆文件（无论哪种 type）都是 <b>Markdown 正文 + 尾部 HTML 注释元数据</b>：</p>
<pre lang="markdown"><code># &lt;content_template 渲染结果&gt;<br/>正文 bullet / 段落...<br/><br/>&lt;!-- MEMORY_FIELDS<br/>{"user":"alice","topic":"code_style","memory_type":"preferences","version":3,"links":[]}<br/>--&gt;</code></pre>
<ul>
<li>正文由 YAML 的 content_template（Jinja2）渲染，变量来自该 memory_type 的字段</li>
<li>元数据即"frontmatter"：identity 字段（user/topic/category/name/event_name…）+ system 字段（memory_type/version/links/backlinks）</li>
<li>directory + filename_template 也是 Jinja2，渲染出最终 URI（如 memories/preferences/alice/code_style.md）</li>
</ul>

<h3>3.3.5 各 memory type 抽取方式速查</h3>
<table>
<colgroup><col width="110"/><col width="70"/><col width="110"/><col width="170"/><col width="180"/><col width="160"/></colgroup>
<thead><tr><th background-color="light-gray"><p>memory type</p></th><th background-color="light-gray"><p>stage</p></th><th background-color="light-gray"><p>抽取 LLM</p></th><th background-color="light-gray"><p>输入材料</p></th><th background-color="light-gray"><p>输出 merge 策略</p></th><th background-color="light-gray"><p>特殊机制</p></th></tr></thead>
<tbody>
<tr><td><p>profile</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息文本</p></td><td><p>content patch</p></td><td><p>强制路由，bullet 带 as-of 日期</p></td></tr>
<tr><td><p>identity</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息</p></td><td><p>content patch</p></td><td><p>init_value 预置</p></td></tr>
<tr><td><p>soul</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息</p></td><td><p>content patch</p></td><td><p>init_value 预置</p></td></tr>
<tr><td><p>preferences</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息</p></td><td><p>user/topic immutable, content patch</p></td><td><p>按话题拆文件</p></td></tr>
<tr><td><p>entities</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息</p></td><td><p>category/name immutable, content patch</p></td><td><p>按实体拆文件</p></td></tr>
<tr><td><p>events</p></td><td><p>user</p></td><td><p>会话抽取</p></td><td><p>会话消息 + ranges 索引</p></td><td><p>全 immutable, add_only</p></td><td><p>正文从原始消息截取，快速路径不合并</p></td></tr>
<tr><td><p>cases</p></td><td><p>user</p></td><td><p>会话抽取（同一次）</p></td><td><p>会话消息（需工具证据）</p></td><td><p>全 immutable</p></td><td><p>与其他 type 同一次 ExtractLoop 产出</p></td></tr>
<tr><td><p>trajectories</p></td><td><p>agent</p></td><td><p>Phase 1 抽取</p></td><td><p>归档会话（含 ToolPart）+ rubric</p></td><td><p>除 content 外 immutable, add_only</p></td><td><p>12 段操作契约，文件名带时间戳</p></td></tr>
<tr><td><p>experiences</p></td><td><p>agent</p></td><td><p>Phase 2 抽取</p></td><td><p>新轨迹正文 + top-5 候选 + top-3 源轨迹</p></td><td><p>name immutable, content/supersedes replace</p></td><td><p>三段式，derived_from 血缘，文件名无时间戳</p></td></tr>
<tr><td><p>tools</p></td><td><p>user</p></td><td><p>默认 disabled</p></td><td><p>—</p></td><td><p>统计 sum, 文本 patch</p></td><td><p>—</p></td></tr>
<tr><td><p>skills</p></td><td><p>user</p></td><td><p>默认 disabled</p></td><td><p>—</p></td><td><p>统计 sum, 文本 patch</p></td><td><p>—</p></td></tr>
</tbody>
</table>
<hr/>

<h1>四、其他 user 子目录</h1>

<h2>4.1 skills/（用户技能资产）</h2>
<ul>
<li>路径：viking://user/{uid}/skills/{skill_name}/SKILL.md。</li>
<li>内容：用户显式要求"记住"的可复用技能（带 SKILL.md 定义）。</li>
<li>与 memories/skills/（使用统计）<b>不同</b>：这里是技能本体，是检索 / 可调用资产。</li>
</ul>

<h2>4.2 peers/（对等 Agent 数据隔离）</h2>
<ul>
<li>路径：viking://user/{uid}/peers/{peer_id}/memories/ 与 .../resources/。</li>
<li>MemoryIsolationHandler 按消息上的 peer_id 把读写路由到对应 peer 的隔离命名空间；自身（self peer）路由回用户主命名空间。</li>
<li>支持 peer_scope（actor / all）控制检索时是否纳入其他对等 Agent 的内容。</li>
</ul>

<h2>4.3 privacy/（隐私配置与版本管理）</h2>
<ul>
<li>路径：viking://user/{uid}/privacy/{category}/{target_key}/。</li>
<li>内容：current.json（当前生效规则）+ .meta.json（元数据，含 latest_version / active_version）+ history/version_*.json（历史版本）。</li>
<li>每次写新规则生成递增版本号，支持回滚；规则会过滤敏感内容，影响记忆提取阶段哪些信息可被写进轨迹 / 经验。</li>
</ul>

<h2>4.4 settings/（用户配置）</h2>
<ul>
<li>路径：viking://user/{uid}/settings/user_config.json。</li>
<li>内容：add_targets（写入默认目标）、memory_policy（启用 / 禁用哪些 memory type、target 开关）等用户级配置。</li>
</ul>

<h2>4.5 resources/（用户私有资源）</h2>
<ul>
<li>路径：viking://user/{uid}/resources/。</li>
<li>内容：用户私有资源文件，走与全局 resources 相同的摄取 / 向量化 / 检索管道，但仅该用户可见。</li>
<li>默认检索范围 = 全局 viking://resources + 用户 user_root（含本目录）。</li>
</ul>
<hr/>

<h1>五、记忆抽取场景</h1>
<p>从 Session 出发，存在多条抽取流水线并行/串行运行：有的是单阶段直接提取（profile/preferences/entities/cases），有的是多阶段逐级蒸馏（session → trajectory → experience），有的无需 LLM 直接记录（events），还有辅助流水线（WM 摘要 + hydrate）。本章按实际触发场景组织。</p>

<h2>5.1 场景全景</h2>
<table>
<colgroup><col width="200"/><col width="170"/><col width="180"/><col width="220"/><col width="60"/></colgroup>
<thead><tr><th background-color="light-gray"><p>场景</p></th><th background-color="light-gray"><p>触发时机</p></th><th background-color="light-gray"><p>覆盖记忆类型</p></th><th background-color="light-gray"><p>核心机制</p></th><th background-color="light-gray"><p>详细说明</p></th></tr></thead>
<tbody>
<tr><td><p><b>场景一：会话进行中实时记住用户信息</b></p></td><td><p>会话进行中，消息累积到批处理阈值</p></td><td><p>profile / preferences / entities 等普通记忆</p></td><td><p>StreamingMemoryUpdater 作为写入合并层，count/time window batching，不是每条消息都做 LLM 抽取</p></td><td><p>5.2</p></td></tr>
<tr><td><p><b>场景二：会话结束后批量抽取记忆（最主要）</b></p></td><td><p>session commit 后</p></td><td><p>profile / identity / soul / preferences / entities / events / cases</p></td><td><p>ExtractLoop + SessionExtractContextProvider 批量抽取；cases 同一次 ExtractLoop 一并产出；events 走 add_only 快速路径</p></td><td><p>5.3</p></td></tr>
<tr><td><p><b>场景三：复杂任务经验沉淀（两阶段）</b></p></td><td><p>Train 管线 rollout 分析阶段</p></td><td><p>trajectories / experiences</p></td><td><p>Phase 1 trajectory（AgentTrajectoryContextProvider，含 ToolPart+rubric）→ Phase 2 experience（AgentExperienceContextProvider，轨迹正文+候选经验对比+derived_from 血缘）；agent stage</p></td><td><p>5.4</p></td></tr>
<tr><td><p><b>场景四：长对话压缩（WM 摘要）</b></p></td><td><p>会话归档时（Phase 2）</p></td><td><p>.overview.md / .abstract.md</p></td><td><p>MemoryUpdater.generate_overview 生成 Working Memory 摘要，7 段固定格式</p></td><td><p>5.5</p></td></tr>
</tbody>
</table>

<h2>5.2 场景一：会话进行中实时记住用户信息</h2>
<p><b>触发时机</b>：会话进行中，消息持续累积。</p>
<p><b>核心机制</b>：StreamingMemoryUpdater 不是"实时 LLM 抽取"——它是写入侧的<b>批处理 / 合并层</b>，接收的已经是抽取完成的 ResolvedOperations。</p>
<ul>
<li>按 (peer_id, memory_type) 做<b>计数 / 时间窗批处理</b>（默认 ≤8 个 op / 10 秒窗口），把多个 commit 的操作攒批写入</li>
<li>operation_mode=add_only（events）→ 快速路径直接写，不合并</li>
<li>其余类型（profile / preferences / entities 等）→ 跨 session / 重复目标时，跑第二次 mini-ExtractLoop（PatchMergeContextProvider, max_iterations=1, 无工具）合并多份提案为唯一 canonical 文件</li>
<li>最终由 MemoryUpdater.apply_operations() 按字段 merge_op 落盘 + 向量化 + 生成 .overview.md</li>
</ul>
<p><b>覆盖记忆类型</b>：profile / identity / soul / preferences / entities 等普通用户记忆（user stage）。</p>
<p><b>设计动机</b>：避免每条消息都触发 LLM 抽取和文件写入，通过批处理降低 IO 和 LLM 调用频率，同时通过 patch 合并保证同一记忆文件的多次更新不会冲突。</p>

<h2>5.3 场景二：会话结束后批量抽取记忆（最主要）</h2>
<p><b>触发时机</b>：session commit 后（post-commit），每次 commit 跑<b>一次</b> ExtractLoop。</p>
<p><b>核心链路</b>：</p>
<pre lang="text"><code>会话 commit/archive 完成<br/>  └─ SessionExtractContextProvider(messages)<br/>       · 组装 "## Conversation History"（默认省略 ToolPart，只保留 TextPart）<br/>       · prefetch：单文件类型 → 直接 read；多文件类型 → 目录语义 search；add_only → 跳过<br/>  └─ ExtractLoop.run()（ReAct，默认 max_iterations=3）<br/>       · system = provider instruction + page_id 规则 + 输出契约（动态 JSON Schema）<br/>       · LLM 可调 read/search 补读，或直接吐出最终 operations<br/>  └─ 输出协议解析（默认 Python DSL，可选 JSON）<br/>  └─ resolve_operations（page_id → URI 绑定）<br/>  └─ StreamingMemoryUpdater 批处理写入</code></pre>
<p><b>覆盖记忆类型与特殊机制</b>：</p>
<table>
<colgroup><col width="200"/><col width="200"/><col width="380"/></colgroup>
<thead><tr><th background-color="light-gray"><p>类型</p></th><th background-color="light-gray"><p>抽取方式</p></th><th background-color="light-gray"><p>特殊机制</p></th></tr></thead>
<tbody>
<tr><td><p>profile / identity / soul</p></td><td><p>同一次 ExtractLoop</p></td><td><p>profile 强制路由（时间性→events、好恶→preferences、第三方→entities），bullet 带 (as of YYYY-MM-DD)</p></td></tr>
<tr><td><p>preferences</p></td><td><p>同一次 ExtractLoop</p></td><td><p>按话题拆文件，user+topic 为主键</p></td></tr>
<tr><td><p>entities</p></td><td><p>同一次 ExtractLoop</p></td><td><p>按实体拆文件，只合并显式别名</p></td></tr>
<tr><td><p>events</p></td><td><p>同一次 ExtractLoop</p></td><td><p>LLM 输出 ranges 指向消息索引，正文从原始消息截取；add_only 走<b>快速路径</b>，不做 patch 合并</p></td></tr>
<tr><td><p>cases</p></td><td><p><b>同一次 ExtractLoop 一并产出</b></p></td><td><p>需含足够工具执行证据，纯闲聊跳过；全 immutable</p></td></tr>
</tbody>
</table>
<p><b>关键认知</b>：</p>
<ul>
<li>cases 与 profile / preferences / events 由<b>同一个 ExtractLoop 一并产出</b>，不是独立 LLM 调用</li>
<li>events 仍有 LLM 抽取（LLM 决定记什么、起什么名、归哪段消息），并非纯机械记录</li>
<li>真正的 LLM 抽取统一在 commit 后跑一次，没有逐消息实时抽取</li>
</ul>

<h2>5.4 场景三：复杂任务经验沉淀（两阶段）</h2>
<p><b>触发时机</b>：Train 管线的 rollout 分析阶段（agent stage，不在用户会话提交时触发）。</p>
<p><b>两阶段架构</b>：</p>
<whiteboard src="WSm6wSo60hmTOwbb6jkclmDzn2e"></whiteboard>
<table>
<colgroup><col width="80"/><col width="220"/><col width="200"/><col width="200"/><col width="90"/></colgroup>
<thead><tr><th background-color="light-gray"><p>阶段</p></th><th background-color="light-gray"><p>Context Provider</p></th><th background-color="light-gray"><p>输入</p></th><th background-color="light-gray"><p>输出</p></th><th background-color="light-gray"><p>写模式</p></th></tr></thead>
<tbody>
<tr><td><p><b>Phase 1</b></p></td><td><p>AgentTrajectoryContextProvider</p></td><td><p>归档消息列表（含 tool_parts）+ rubric 评估</p></td><td><p>memories/trajectories/*.md</p></td><td><p>add_only</p></td></tr>
<tr><td><p><b>Phase 2</b></p></td><td><p>AgentExperienceContextProvider</p></td><td><p>新轨迹 summary + top-5 候选经验 + top-3 候选的源轨迹</p></td><td><p>memories/experiences/*.md + derived_from 链接</p></td><td><p>upsert</p></td></tr>
</tbody>
</table>
<p>设计动机：先把"这次怎么做的"固化成轨迹（结构化、可追溯），再在轨迹之上泛化出"以后遇到类似情况怎么做"的经验（可注入 prompt 的规则）。</p>

<h3>Phase 1：Trajectory 提取</h3>
<ul>
<li><b>触发时机</b>：TrajectoryRolloutAnalyzer.analyze() 中，先跑 rubric 评估，再启动轨迹提取。</li>
<li><b>原始内容（输入）</b>：归档会话消息列表（含 user/assistant 的 TextPart + ToolPart，include_tool_parts_in_conversation=True）；ToolCall 格式化为单行：tool_name / status / input / output 前 500 字符 / duration_ms；<b>额外注入 rubric 评估反馈</b>（passed / score / feedback / criterion_results）作为末尾 user 消息；LLM 无工具（get_tools()→[]），所有材料一次性 prefetch</li>
<li><b>抽取提示词</b>：system = provider instruction（"You are an extraction agent. Analyze the archived conversation… output only JSON that matches the schema descriptions."）+ page_id 规则 + read 格式规则 + 输出契约（SchemaModelGenerator 从 YAML 动态生成的 Pydantic JSON Schema）；无 few-shot 示例；关键约束：一个记录 = 一个可复用操作边界，禁止伞形记录，必须泛化（去 ID/人名/日期/金额/路径）</li>
<li><b>抽取结果</b>：路径 memories/trajectories/{trajectory_name}_{session_timestamp}.md（add_only，文件名带时间戳）；frontmatter 5 字段：trajectory_name（immutable，snake_case ≤5 词）、outcome（immutable，枚举 success/failure/partial/unfinished/unknown）、task_query（immutable，可独立复跑的用户任务原文）、retrieval_anchor（immutable，固定四段 Stage; Boundary; Capability; Target）、content（patch，12 段操作契约）；embedding 只编码 trajectory_name + retrieval_anchor；正文 12 段操作契约（固定顺序）：Domain / Trigger / Operation Intent（Family + Primary object + Target lifecycle）/ Preconditions / Immutable Object Boundary / Procedure / Write Field Provenance / Anti-patterns / Applicability Boundary / Negative Applicability / Result / Evidence</li>
</ul>

<h3>Phase 2：Experience 提取</h3>
<ul>
<li><b>触发时机</b>：Phase 1 产出新轨迹之后，ExperienceGradientEstimator.estimate() 对每条轨迹并行跑一次 Phase 2。</li>
<li><b>原始内容（输入）</b>：① 新轨迹的<b>正文全文</b>（trajectory.content = 12 段操作契约，非 frontmatter）作为 trajectory_summary；② 向量检索 top-5 候选经验（search_files(query=summary[:500], limit=5)，无结果时退化为 ls 目录列举）；③ top-3 候选经验的 derived_from 源轨迹（每条经验最多加载最近 3 条源轨迹全文）；④ 原始会话历史；所有材料用伪 read 调用包装（context_role 标记 new_trajectory / candidate_experience / candidate_source_trajectory），LLM 无工具</li>
<li><b>抽取提示词</b>：system = "memory extraction agent" 角色指令：从轨迹蒸馏经验，每个不同用户意图输出<b>独立</b>一条 entry（Split over merge）；系统自动处理：同名→in-place update、新名→create、supersedes 非空→删除旧经验并继承历史；<b>禁止用 delete_ids</b>，用 supersedes 代替；Approach 超 8 个 bullet 必须拆分；所有内容用检测到的输出语言；只输出 JSON</li>
<li><b>抽取结果</b>：路径 memories/experiences/{experience_name}.md（upsert，文件名<b>无时间戳</b>，同名即覆盖）；字段：experience_name（immutable，snake_case ≤5 词）、content（replace，三段式 ## Situation / ## Approach / ## Reflect，严格互斥：Approach 只放正向动作，Reflect 只放负面约束）、supersedes（replace，仅当用更宽名字取代旧经验时填旧名）；同时建立 derived_from 血缘链接（experience → trajectory）</li>
</ul>

<h3>血缘追踪机制（derived_from link）</h3>
<ul>
<li>每条经验通过 derived_from 类型的 <b>StoredLink</b> 指向它提炼自的源轨迹。</li>
<li>链接存于经验文件的 links 字段：{from_uri, to_uri, link_type: "derived_from", weight}。</li>
<li>AgentExperienceContextProvider._load_source_trajectories() 从候选经验的 links 里挑出 link_type == "derived_from" 的 to_uri，加载对应轨迹作为参考材料。</li>
<li>作用：让"经验 ← 轨迹"的来源可追溯；当经验被 supersedes 重命名时，旧经验的轨迹历史会被继承到新经验上。</li>
</ul>

<h2>5.5 场景四：长对话压缩（WM 摘要）</h2>
<p><b>触发时机</b>：会话归档时（Phase 2 异步执行），与长期记忆抽取并行启动。</p>
<p><b>核心机制</b>：</p>
<ul>
<li>MemoryUpdater.generate_overview 生成 Working Memory 摘要</li>
<li>创建模板：openviking/prompts/templates/compression/ov_wm_v2.yaml</li>
<li>增量模板：ov_wm_v2_update.yaml（增量模式强制通过 update_working_memory 工具按 KEEP / APPEND / REPLACE 合并）</li>
<li>输出固定为 <b>7 段</b> Working Memory Markdown，写入 archive_NNN/.overview.md：</li>
</ul>
<ol>
<li>Session Title</li>
<li>Current State（最高优先级，永远反映最新状态）</li>
<li>Task &amp; Goals</li>
<li>Key Facts &amp; Decisions（关于用户世界的持久事实）</li>
<li>Files &amp; Context（被引用的资源）</li>
<li>Errors &amp; Corrections</li>
<li>Open Issues</li>
</ol>
<ul>
<li>额外生成 .abstract.md（摘要的再提炼），用于目录级语义检索</li>
</ul>
<p><b>与记忆抽取的关系</b>：WM 摘要是会话级的短期记忆压缩，服务于后续会话的上下文恢复；长期记忆抽取（场景二、三）则是把可复用的知识沉淀到 memories/ 命名空间。两者在 Phase 2 中并行执行。</p>

<h2>5.6 通用机制：记忆合并策略</h2>
<p>不同 memory type / 字段按 YAML 中声明的 merge_op 合并：</p>
<table>
<colgroup><col width="110"/><col width="280"/><col width="240"/><col width="150"/></colgroup>
<thead><tr><th background-color="light-gray"><p>策略</p></th><th background-color="light-gray"><p>适用字段</p></th><th background-color="light-gray"><p>行为</p></th><th background-color="light-gray"><p>典型场景</p></th></tr></thead>
<tbody>
<tr><td><p><b>PATCH</b></p></td><td><p>content（profile / preferences / entities / trajectory content）</p></td><td><p>SEARCH/REPLACE/DELETE 块级编辑，不全量重写</p></td><td><p>增量补全画像、偏好</p></td></tr>
<tr><td><p><b>REPLACE</b></p></td><td><p>content（experiences）、supersedes</p></td><td><p>全量重写该字段</p></td><td><p>经验整体重写为最新规则</p></td></tr>
<tr><td><p><b>SUM</b></p></td><td><p>call_count / success_count 等统计</p></td><td><p>数值累加</p></td><td><p>tools/skills 使用统计</p></td></tr>
<tr><td><p><b>IMMUTABLE</b></p></td><td><p>experience_name / trajectory_name / outcome / 事件标识</p></td><td><p>只设一次，后续不可改</p></td><td><p>命名与定性，防漂移</p></td></tr>
</tbody>
</table>
<p><b>add_only vs upsert</b>：</p>
<table>
<colgroup><col width="120"/><col width="240"/><col width="420"/></colgroup>
<thead><tr><th background-color="light-gray"><p>模式</p></th><th background-color="light-gray"><p>用于</p></th><th background-color="light-gray"><p>理由</p></th></tr></thead>
<tbody>
<tr><td><p>add_only</p></td><td><p>trajectories / events</p></td><td><p>历史事实与执行契约一旦产出不应被涂改，保留全部原始版本</p></td></tr>
<tr><td><p>upsert</p></td><td><p>experiences / profile / preferences</p></td><td><p>经验和偏好需要随认知更新而重写 / 补全，避免无限膨胀</p></td></tr>
</tbody>
</table>
<p><b>supersedes 机制</b>：</p>
<table>
<colgroup><col width="200"/><col width="220"/><col width="360"/></colgroup>
<thead><tr><th background-color="light-gray"><p>操作</p></th><th background-color="light-gray"><p>触发</p></th><th background-color="light-gray"><p>行为</p></th></tr></thead>
<tbody>
<tr><td><p>同名 experience 重写</p></td><td><p>Phase 2 输出相同 experience_name</p></td><td><p>content 整体 replace 重写</p></td></tr>
<tr><td><p>新经验替代旧经验</p></td><td><p>Phase 2 设置不同 supersedes</p></td><td><p>旧经验自动删除，其轨迹历史继承到新经验</p></td></tr>
<tr><td><p>偏好 / 画像增量补全</p></td><td><p>patch 类字段</p></td><td><p>SEARCH/REPLACE/DELETE 块级编辑</p></td></tr>
<tr><td><p>轨迹 / 事件追加</p></td><td><p>add_only 类</p></td><td><p>只新增文件，不修改历史</p></td></tr>
</tbody>
</table>
<hr/>

<h1>六、消费与维护</h1>

<h2>6.1 向量检索召回</h2>
<ul>
<li>记忆文件（experiences / trajectories / events / entities 等）在写入时被向量化入 VikingDB。</li>
<li>每条轨迹有专门的 retrieval_anchor（为 embedding 而非展示设计的正向检索文本），格式如 Stage: …; Boundary: …; Capability: …; Target: …。</li>
<li>后续会话按当前意图做向量检索，命中相关经验 / 轨迹后召回。</li>
</ul>

<h2>6.2 上下文自动注入（各 Context Provider 职责）</h2>
<ul>
<li>对话进行时无需显式调用工具，ContextAssembler 自动从 user 命名空间召回相关内容注入。</li>
<li>各 Provider 分工：身份 / 画像类在会话早期注入作为基线；经验类按当前意图召回作为可执行规则；事件类按时间 / 主题补充背景。</li>
<li>受 token 配额、详情层级（abstract / overview / full）、跨轮去重、peer 范围等控制。</li>
</ul>

<h2>6.3 Train 子系统如何消费 trajectory</h2>
<p>离线策略优化管线以轨迹为训练信号：</p>
<pre lang="text"><code>CaseLoader → PolicySnapshotter → RolloutExecutor<br/>          → TrajectoryRolloutAnalyzer → GradientEstimator<br/>          → PolicyOptimizer → PolicyUpdater</code></pre>
<ul>
<li>TrajectoryRolloutAnalyzer 复用 AgentTrajectoryContextProvider 做轨迹分析；</li>
<li>GradientEstimator 复用 AgentExperienceContextProvider 做经验级别的信号估计；</li>
<li>SessionCommitPolicyTrainer 把 rollout 写回 session、走 commit 抽取，形成"训练回流"闭环；</li>
<li>轨迹被读回作为 gradient / policy 的训练信号。</li>
</ul>

<h2>6.4 记忆更新与 supersedes 机制</h2>
<table>
<colgroup><col width="200"/><col width="220"/><col width="360"/></colgroup>
<thead><tr><th background-color="light-gray"><p>操作</p></th><th background-color="light-gray"><p>触发</p></th><th background-color="light-gray"><p>行为</p></th></tr></thead>
<tbody>
<tr><td><p>同名 experience 重写</p></td><td><p>Phase 2 输出相同 experience_name</p></td><td><p>content 整体 replace 重写</p></td></tr>
<tr><td><p>新经验替代旧经验</p></td><td><p>Phase 2 设置不同 supersedes</p></td><td><p>旧经验自动删除，其轨迹历史继承到新经验</p></td></tr>
<tr><td><p>偏好 / 画像增量补全</p></td><td><p>patch 类字段</p></td><td><p>SEARCH/REPLACE/DELETE 块级编辑</p></td></tr>
<tr><td><p>轨迹 / 事件追加</p></td><td><p>add_only 类</p></td><td><p>只新增文件，不修改历史</p></td></tr>
</tbody>
</table>

<h2>6.5 各子目录的保留策略</h2>
<table>
<colgroup><col width="240"/><col width="540"/></colgroup>
<thead><tr><th background-color="light-gray"><p>目录</p></th><th background-color="light-gray"><p>保留策略</p></th></tr></thead>
<tbody>
<tr><td><p>sessions/（原始消息）</p></td><td><p><b>只增不减，无 TTL，永不物理删除</b>；归档完整保留</p></td></tr>
<tr><td><p>memories/trajectories/</p></td><td><p>add_only，永久累积</p></td></tr>
<tr><td><p>memories/experiences/</p></td><td><p>upsert，旧经验经 supersedes 被替换删除</p></td></tr>
<tr><td><p>memories/events/</p></td><td><p>add_only，永久累积</p></td></tr>
<tr><td><p>memories/tools|skills/</p></td><td><p>默认 disabled，开启后统计累加</p></td></tr>
<tr><td><p>privacy/</p></td><td><p>版本历史永久保留，current 可切回任意版本</p></td></tr>
</tbody>
</table>

<h2>6.6 隐私过滤对记忆提取的影响</h2>
<p>privacy/ 中的过滤规则在记忆提取前生效：被标记为隐私 / 敏感的内容不会被写进 trajectory 或 experience 的正文。提取 Prompt 本身也要求去掉凭据、令牌、联系方式等敏感信息，双保险防止记忆侧泄漏原始会话中的敏感数据。</p>
<hr/>

<h1>七、关键设计决策</h1>

<h2>7.1 Session 实时写入 vs 异步归档</h2>
<ul>
<li><b>实时写入</b>保证消息不丢、低延迟；<b>异步归档</b>把慢 LLM 摘要 / 记忆抽取移出用户请求路径。</li>
<li>Phase 1 同步持锁只做切分与重写（快），Phase 2 全部重活放后台 QueueFS，重启可恢复。</li>
</ul>

<h2>7.2 Trajectory 作为 memory type 而非 session 视图</h2>
<ul>
<li>轨迹被建模成独立 memory type（stage: agent、agent_only: true、add_only），而不是对 session 的实时投影。</li>
<li>好处：轨迹是<b>沉淀产物</b>，有稳定命名、可独立向量化、可作为经验血缘来源；不会随 session 后续演进被漂移改写。</li>
</ul>

<h2>7.3 两阶段提取（先 trajectory 后 experience）</h2>
<ul>
<li>先轨迹（结构化操作契约、可追溯、不合并），再经验（在轨迹上泛化、可注入 prompt、允许重写）。</li>
<li>经验提取时带向量候选 + 源轨迹 grounding，避免重复造轮子，并保留"经验从哪来"的血缘。</li>
</ul>

<h2>7.4 add_only vs upsert 的设计考量</h2>
<table>
<colgroup><col width="120"/><col width="240"/><col width="420"/></colgroup>
<thead><tr><th background-color="light-gray"><p>模式</p></th><th background-color="light-gray"><p>用于</p></th><th background-color="light-gray"><p>理由</p></th></tr></thead>
<tbody>
<tr><td><p>add_only</p></td><td><p>trajectories / events</p></td><td><p>历史事实与执行契约一旦产出不应被涂改，保留全部原始版本</p></td></tr>
<tr><td><p>upsert</p></td><td><p>experiences / profile / preferences</p></td><td><p>经验和偏好需要随认知更新而重写 / 补全，避免无限膨胀</p></td></tr>
</tbody>
</table>

<h2>7.5 原始消息永不删除（archive 只增不减）</h2>
<ul>
<li>归档消息完整保存在 archive_NNN/messages.jsonl，"删除"只作用于记忆索引与向量，不动原始会话数据。</li>
<li>保证可回溯、可重放、可重新提取；记忆可以被 supersedes 替换，但历史执行事实永远可查。</li>
</ul>
<hr/>

<h1>八、快速参考卡</h1>

<h2>常用操作速查</h2>
<table>
<colgroup><col width="240"/><col width="540"/></colgroup>
<thead><tr><th background-color="light-gray"><p>操作</p></th><th background-color="light-gray"><p>说明</p></th></tr></thead>
<tbody>
<tr><td><p>查看活跃会话消息</p></td><td><p>读 viking://user/{uid}/sessions/{sid}/messages.jsonl</p></td></tr>
<tr><td><p>查看某次提交摘要</p></td><td><p>读 .../history/archive_NNN/.overview.md</p></td></tr>
<tr><td><p>查看已沉淀轨迹</p></td><td><p>浏览 memories/trajectories/</p></td></tr>
<tr><td><p>查看可复用经验</p></td><td><p>浏览 memories/experiences/</p></td></tr>
<tr><td><p>触发 / 调整 auto-commit</p></td><td><p>会话配置 pending_token_threshold 等阈值</p></td></tr>
<tr><td><p>关闭某类记忆</p></td><td><p>settings/user_config.json 的 memory_policy</p></td></tr>
<tr><td><p>隐私规则回滚</p></td><td><p>privacy/.../history/version_*.json 切回版本</p></td></tr>
</tbody>
</table>

<h2>关键代码位置索引</h2>
<table>
<colgroup><col width="280"/><col width="500"/></colgroup>
<thead><tr><th background-color="light-gray"><p>模块</p></th><th background-color="light-gray"><p>路径</p></th></tr></thead>
<tbody>
<tr><td><p>auto-commit 策略</p></td><td><p>openviking/session/auto_commit_policy.py</p></td></tr>
<tr><td><p>保留规划器（纯）</p></td><td><p>openviking/session/retention.py</p></td></tr>
<tr><td><p>会话读写 / 提交编排</p></td><td><p>openviking/session/session.py</p></td></tr>
<tr><td><p>Phase 2 队列消费者</p></td><td><p>openviking/storage/queuefs/session_commit_processor.py</p></td></tr>
<tr><td><p>抽取 ReAct 主循环</p></td><td><p>openviking/session/memory/extract_loop.py</p></td></tr>
<tr><td><p>会话抽取 Context Provider</p></td><td><p>openviking/session/memory/session_extract_context_provider.py</p></td></tr>
<tr><td><p>patch 合并 Context Provider</p></td><td><p>openviking/session/memory/patch_merge_context_provider.py</p></td></tr>
<tr><td><p>写入批处理 / 合并</p></td><td><p>openviking/session/memory/streaming_memory_updater.py</p></td></tr>
<tr><td><p>落盘执行</p></td><td><p>openviking/session/memory/memory_updater.py</p></td></tr>
<tr><td><p>Phase 1 轨迹提取 Provider</p></td><td><p>openviking/session/memory/agent_trajectory_context_provider.py</p></td></tr>
<tr><td><p>Phase 2 经验提取 Provider</p></td><td><p>openviking/session/memory/agent_experience_context_provider.py</p></td></tr>
<tr><td><p>经验→轨迹血缘</p></td><td><p>openviking/session/memory/experience_lineage.py</p></td></tr>
<tr><td><p>动态输出 schema 生成</p></td><td><p>openviking/session/memory/schema_model_generator.py</p></td></tr>
<tr><td><p>输出协议（JSON / Python）</p></td><td><p>openviking/session/memory/extraction_output_protocol/</p></td></tr>
<tr><td><p>memory type 注册表</p></td><td><p>openviking/session/memory/memory_type_registry.py</p></td></tr>
<tr><td><p>合并策略（patch/replace/sum/immutable）</p></td><td><p>openviking/session/memory/merge_op/</p></td></tr>
<tr><td><p>记忆类型 YAML</p></td><td><p>openviking/prompts/templates/memory/*.yaml</p></td></tr>
<tr><td><p>WM v2 摘要 Prompt</p></td><td><p>openviking/prompts/templates/compression/ov_wm_v2.yaml</p></td></tr>
<tr><td><p>对等隔离路由</p></td><td><p>openviking/session/memory/memory_isolation_handler.py</p></td></tr>
<tr><td><p>隐私配置存储</p></td><td><p>openviking/privacy/service.py</p></td></tr>
<tr><td><p>Train 管线</p></td><td><p>openviking/session/train/</p></td></tr>
</tbody>
</table>
<hr/>
<p align="center"><em>文档版本：v1.2 | 最后更新：2026-09-16</em></p>

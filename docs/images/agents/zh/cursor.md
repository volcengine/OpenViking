```bash
bash <(curl -fsSL https://ovrelease.tos-cn-beijing.volces.com/memory-plugin-shared/install.sh) --harness cursor --dist tos
```

选 **火山引擎 OpenViking 云服务**，把本页 API Key 贴进去。

重启 Cursor。在 **Tools & MCPs** 里确认 `openviking` 已连接。

name: PR Review (Disabled)

on:
  workflow_dispatch:

jobs:
  pr_review_disabled:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Disabled pending hardened PR review workflow
        run: |
          echo "PR Agent workflow is temporarily disabled."
          echo "Reason: unpinned third-party review action with elevated repository permissions is disabled."

[project]
name = "openviking-mcp-bridge"
version = "0.1.0"
description = "Thin MCP bridge to OpenViking HTTP API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.8.0",
]

[tool.uv]
dev-dependencies = []
